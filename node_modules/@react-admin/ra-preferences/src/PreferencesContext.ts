import { createContext } from 'react';

/**
 * A context for preferences related configuration.
 */
export const PreferencesContext = createContext(undefined);

export type PreferencesContextValue = {
    synchronize: boolean;
    updatePreferences: <T extends unknown = any>(
        data: T,
        previousData: T
    ) => void;
};
