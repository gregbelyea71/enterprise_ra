import { ReactNode } from 'react';
import { OnFailure, OnSuccess } from 'ra-core';
/**
 * A provider for PreferencesContext which gives access to preferences related configuration. It also preloads the currently logged user preferences.
 * @param props
 * @param {ReactNode} props.children
 * @param {boolean} props.synchronize A boolean indicating whether preferences should be synchronized with the backend using the dataProvider.
 */
export declare const PreferencesContextProvider: ({ children, onSuccess, onFailure, synchronize, }: PreferencesContextProviderProps) => JSX.Element;
export declare type PreferencesContextProviderProps = {
    children: ReactNode;
    onSuccess?: OnSuccess;
    onFailure?: OnFailure;
    synchronize?: boolean;
};
//# sourceMappingURL=PreferencesContextProvider.d.ts.map