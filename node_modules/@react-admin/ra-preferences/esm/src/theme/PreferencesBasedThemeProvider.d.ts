import { ReactNode } from 'react';
export interface PreferencesBasedThemeProviderProps {
    children: ReactNode;
    themeFromType?: (type: 'light' | 'dark') => any;
}
/**
 * Inject a MUI theme object to its child component based on the theme in preferences
 *
 * @example
 *
 *     export const App = () => (
 *         <PreferencesBasedThemeProvider>
 *             <Admin dataProvider={dataProvider}>
 *                 <Resource name="posts" list={PostList} />
 *             </Admin>
 *         </PreferencesBasedThemeProvider>
 *     );
 *
 * @param {function} themeFromType Function returning a MUI Theme based on the theme string stored in preferences. Defaults to type => ({ palette: { type } }).
 *
 * Additional props are passed down to the child component.
 */
export declare const PreferencesBasedThemeProvider: ({ children, themeFromType, ...rest }: PreferencesBasedThemeProviderProps) => import("react").ReactElement<any, string | import("react").JSXElementConstructor<any>>;
//# sourceMappingURL=PreferencesBasedThemeProvider.d.ts.map