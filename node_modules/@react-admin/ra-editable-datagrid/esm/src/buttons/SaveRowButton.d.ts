/// <reference types="react" />
import { MutationMode } from 'react-admin';
export declare const SaveRowButton: ({ dirty, handleSubmit, invalid, mutationMode, quitEditMode, saving, undoable, }: SaveRowButtonProps) => JSX.Element;
export interface SaveRowButtonProps {
    dirty: boolean;
    handleSubmit: () => void;
    invalid: boolean;
    mutationMode?: MutationMode;
    quitEditMode?: () => void;
    saving?: boolean;
    undoable?: boolean;
}
//# sourceMappingURL=SaveRowButton.d.ts.map