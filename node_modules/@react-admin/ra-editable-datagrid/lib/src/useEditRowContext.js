"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useEditRowContext = void 0;
var react_1 = require("react");
var EditRowContext_1 = require("./EditRowContext");
/**
 * A hook which returns a function to open or quit the edit mode of a row from the sideEffect of a custom form inside the <EditableDatagrid>.
 *
 * @example
 *     const ArtistList = props => (
 *         <List {...props} hasCreate>
 *             <EditableDatagrid
 *                 undoable
 *                 editForm={<ArtistForm />}
 *             >
 *                 <TextField source="id" />
 *                 <TextField source="firstname" />
 *                 <TextField source="name" />
 *                 <DateField source="dob" label="born" />
 *                 <SelectField
 *                     source="prof"
 *                     label="Profession"
 *                     choices={professionChoices}
 *                 />
 *             </EditableDatagrid>
 *         </List>
 *     );
 *
 *     const ArtistForm: FC = props => {
 *         const { close } = useEditRowContext();
 *
 *         return (
 *             <RowForm onSuccess={() => close()} {...props}>
 *                 <TextField source="id" />
 *                 <TextInput source="firstname" validate={required()} />
 *                 <TextInput source="name" validate={required()} />
 *                 <DateInput source="dob" label="born" validate={required()} />
 *                 <SelectInput
 *                     source="prof"
 *                     label="Profession"
 *                     choices={professionChoices}
 *                 />
 *             </RowForm>
 *         );
 *     };
 */
var useEditRowContext = function () {
    var context = react_1.useContext(EditRowContext_1.EditRowContext);
    if (!context && process.env.NODE_ENV === 'development') {
        console.error('useEditRowContext cannot be called outside of the RowForm passed as the `editForm` of the <EditableDatagrid> row.');
    }
    return context;
};
exports.useEditRowContext = useEditRowContext;
