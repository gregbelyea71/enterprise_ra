var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
/* eslint-disable jsx-a11y/no-autofocus */
import React from 'react';
import { Admin, Resource, SimpleForm, TextInput, DateTimeInput, } from 'react-admin';
import { createMemoryHistory } from 'history';
import { createMuiTheme } from '@material-ui/core/styles';
import { CompleteCalendar } from '../src';
import dataProvider from './dataProvider';
export default { title: 'ra-calendar/CompleteCalendar' };
var EventList = function (props) { return (React.createElement(CompleteCalendar, __assign({}, props),
    React.createElement(SimpleForm, null,
        React.createElement(TextInput, { source: "title", autoFocus: true }),
        React.createElement(DateTimeInput, { source: "start" }),
        React.createElement(DateTimeInput, { source: "end" })))); };
export var Basic = function () { return (React.createElement(Admin, { history: createMemoryHistory(), dataProvider: dataProvider },
    React.createElement(Resource, { name: "events", list: EventList }))); };
export var DarkMode = function () { return (React.createElement(Admin, { history: createMemoryHistory(), dataProvider: dataProvider, theme: createMuiTheme({ palette: { type: 'dark' } }) },
    React.createElement(Resource, { name: "events", list: EventList }))); };
