"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListActions = void 0;
var React = __importStar(require("react"));
var react_1 = require("react");
var react_admin_1 = require("react-admin");
var usePermissions_1 = require("../usePermissions");
var canAccess_1 = require("../canAccess");
/**
 * Replacement for react-admin's ListAction that adds RBAC control to actions
 *
 * Users must have the 'create' permission on the resource to see the CreateButton.
 * Users must have the 'export' permission on the resource to see the ExportButton.
 *
 * @example
 * import { List } from 'react-admin';
 * import { ListActions } from '@react-admin/ra-rbac';
 *
 * export const PostList = (props) => (
 *     <List actions={<ListActions />} {...props}>
 *         ...
 *     </List>
 * );
 */
var ListActions = function (props) {
    var className = props.className, exporter = props.exporter, filtersProp = props.filters, rest = __rest(props, ["className", "exporter", "filters"]);
    var _a = react_admin_1.useListContext(props), currentSort = _a.currentSort, displayedFilters = _a.displayedFilters, filterValues = _a.filterValues, basePath = _a.basePath, selectedIds = _a.selectedIds, showFilter = _a.showFilter, total = _a.total;
    var resource = react_admin_1.useResourceContext(rest);
    var hasCreate = react_admin_1.useResourceDefinition(rest).hasCreate;
    var filters = react_1.useContext(react_admin_1.FilterContext) || filtersProp;
    var _b = usePermissions_1.usePermissions(), loaded = _b.loaded, permissions = _b.permissions;
    return react_1.useMemo(function () {
        return loaded && (React.createElement(react_admin_1.TopToolbar, __assign({ className: className }, react_admin_1.sanitizeListRestProps(rest)),
            filtersProp
                ? react_1.cloneElement(filtersProp, {
                    resource: resource,
                    showFilter: showFilter,
                    displayedFilters: displayedFilters,
                    filterValues: filterValues,
                    context: 'button',
                })
                : filters && React.createElement(react_admin_1.FilterButton, null),
            hasCreate &&
                canAccess_1.canAccess({
                    action: 'create',
                    resource: resource,
                    permissions: permissions,
                }) && React.createElement(react_admin_1.CreateButton, { basePath: basePath }),
            exporter !== false &&
                canAccess_1.canAccess({
                    action: 'export',
                    resource: resource,
                    permissions: permissions,
                }) && React.createElement(react_admin_1.CreateButton, { basePath: basePath }) && (React.createElement(react_admin_1.ExportButton, { disabled: total === 0, resource: resource, sort: currentSort, filterValues: filterValues }))));
    }, 
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
        resource,
        displayedFilters,
        filterValues,
        selectedIds,
        filters,
        total,
        loaded,
    ]);
};
exports.ListActions = ListActions;
exports.ListActions.propTypes = react_admin_1.ListActions.propTypes;
exports.ListActions.defaultProps = react_admin_1.ListActions.defaultProps;
