var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import React from 'react';
import { Admin, Resource, List, Datagrid, Show, TabbedShowLayout, TextField, } from 'react-admin';
import fakeRestDataProvider from 'ra-data-fakerest';
import generateData from 'data-generator-retail';
import { Tab } from '../../src';
export default { title: 'ra-rbac/detail/Tab' };
var data = generateData();
var dataProvider = fakeRestDataProvider(data, true);
var authProvider = {
    checkAuth: function () { return Promise.resolve(); },
    login: function () { return Promise.resolve(); },
    logout: function () { return Promise.resolve(); },
    checkError: function () { return Promise.resolve(); },
    getPermissions: function () {
        return promiseFor({
            permissions: [
                { action: ['list', 'show'], resource: 'products' },
                { action: 'read', resource: 'products.reference' },
                { action: 'read', resource: 'products.width' },
                { action: 'read', resource: 'products.height' },
                // 'products.description' is missing
                { action: 'read', resource: 'products.thumbnail' },
                // 'products.image' is missing
                { action: 'read', resource: 'products.tab.description' },
                { action: 'read', resource: 'products.tab.images' },
                // 'products.tab.stock' is missing
            ],
        });
    },
};
var ProductList = function (props) { return (React.createElement(List, __assign({}, props),
    React.createElement(Datagrid, { rowClick: "show" },
        React.createElement(TextField, { source: "id" }),
        React.createElement(TextField, { source: "reference" })))); };
var ProductShow = function (props) { return (React.createElement(Show, __assign({}, props),
    React.createElement(TabbedShowLayout, null,
        React.createElement(Tab, { label: "Description", name: "description" },
            React.createElement(TextField, { source: "reference" }),
            React.createElement(TextField, { source: "width" }),
            React.createElement(TextField, { source: "height" }),
            React.createElement(TextField, { source: "description" })),
        React.createElement(Tab, { label: "Images", name: "images" },
            React.createElement(TextField, { source: "image" }),
            React.createElement(TextField, { source: "thumbnail" })),
        React.createElement(Tab, { label: "Stock", name: "stock" },
            React.createElement(TextField, { source: "stock" }))))); };
export var Basic = function () { return (React.createElement(Admin, { authProvider: authProvider, dataProvider: dataProvider },
    React.createElement(Resource, { name: "products", list: ProductList, show: ProductShow }),
    React.createElement(Resource, { name: "categories" }))); };
var promiseFor = function (result) {
    return new Promise(function (resolve) {
        setTimeout(function () {
            resolve(result);
        }, 300);
    });
};
