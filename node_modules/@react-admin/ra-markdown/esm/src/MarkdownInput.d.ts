import React from 'react';
import { InputProps } from 'react-admin';
import 'codemirror/lib/codemirror.css';
import '@toast-ui/editor/dist/toastui-editor.css';
import { Editor } from '@toast-ui/react-editor';
export interface MarkdownInputProps extends Partial<Omit<React.ComponentProps<typeof Editor>, 'onChange' | 'onBlur' | 'onFocus'>>, InputProps {
    helperText?: string;
    label?: string;
    fullWidth?: boolean;
}
/**
 * An Input component for markdown content
 *
 * @example
 *
 *    import { Edit, SimpleForm, TextInput } from 'react-admin';
 *    import { MarkdownInput } from '@react-admin/ra-markdown';
 *
 *    const PostEdit = props => (
 *        <Edit {...props}>
 *            <SimpleForm>
 *                <TextInput source="title" />
 *                <MarkdownInput source="description" />
 *            </SimpleForm>
 *        </Edit>
 *    );
 *
 * Based on the '@toast-ui/react-editor' package.
 *
 * @example
 *
 *    const options = {
 *        previewStyle: 'tab',
 *        height: 'auto',
 *        initialEditType: 'markdown',
 *        hideModeSwitch: true,
 *    };
 *    <MarkdownInput source="description" options={options} />
 *
 * The object passed as `options` props is passed to the <Editor> component
 * @see https://nhn.github.io/tui.editor/latest/ToastUIEditor
 */
export declare const MarkdownInput: {
    ({ fullWidth, helperText, label, options, resource, source, ...rest }: MarkdownInputProps): JSX.Element;
    defaultProps: {
        fullWidth: boolean;
        options: {
            previewStyle: string;
            height: string;
            initialEditType: string;
            useCommandShortcut: boolean;
        };
    };
};
//# sourceMappingURL=MarkdownInput.d.ts.map