export interface RaTourTranslationMessages {
    'ra-tour': {
        action: {
            last: string;
            next: string;
            skip: string;
            open: string;
        };
    };
}
