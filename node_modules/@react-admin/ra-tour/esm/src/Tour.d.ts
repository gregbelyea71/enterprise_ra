/// <reference types="react" />
import { TourType } from './types';
interface TourProps {
    joyrideProps?: any;
    tours: {
        [id: string]: TourType;
    };
}
/**
 * Component that handles the UI part of the tour.
 *
 * Uses react-joyride
 *
 * @param {TourType} tours List of supported tours
 * @param {any} joyrideProps Props passed down to react-joyride
 */
declare const Tour: ({ tours, joyrideProps }: TourProps) => JSX.Element;
export default Tour;
//# sourceMappingURL=Tour.d.ts.map