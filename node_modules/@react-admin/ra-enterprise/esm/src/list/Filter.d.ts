import { FC, ReactNode } from 'react';
import { ClassesOverride } from 'react-admin';
declare const useStyles: (props?: any) => import("@material-ui/styles").ClassNameMap<"button" | "form">;
export interface FilterProps {
    children: ReactNode;
    classes?: ClassesOverride<typeof useStyles>;
    context?: 'form' | 'button';
    variant?: string;
}
/**
 * A customized version of the Filter button/form combo from react-admin which
 * uses our custom FilterForm. This fixes the issue where the filter form is taking
 * too much vertical space even though there are no filters.
 *
 * @example
 *
 * const PostFilter = (props) => (
 *     <Filter {...props}>
 *         <TextInput label="Search" source="q" alwaysOn />
 *         <TextInput label="Title" source="title" defaultValue="Hello, World!" />
 *     </Filter>
 * );
 *
 * export const PostList = (props) => (
 *     <List {...props} filters={<PostFilter />}>
 *         ...
 *     </List>
 * );
 *
 */
export declare const Filter: FC<FilterProps>;
export {};
//# sourceMappingURL=Filter.d.ts.map